@model IEnumerable<WEBAPP.Models.Stream>

@{
    ViewBag.Title = "StreamList";
}

<h2>StreamList</h2>

<p>
    @Html.ActionLink("Create New", "AddStream","",new {@class="btn btn-sm btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StreamName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CourseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedOn)
        </th>
     
       
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StreamName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedOn)
        </td>
 
        <td>
            @Html.ActionLink("Edit", "EditStream", new { id=item.Id },new {@class="btn btn-sm btn-success" }) 
            @*@Html.ActionLink("Details", "Details", new { id=item.Id },new {@class="btn btn-sm btn-success" }) 
            @Html.ActionLink("Delete", "Delete", new { id=item.Id },new {@class="btn btn-sm btn-success" })*@
        </td>
    </tr>
}

</table>
